# Track SQLite state databases via Git LFS (if they ever get committed)
*.db filter=lfs diff=lfs merge=lfs -text

# Ensure consistent line endings for text files
*.md text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.json text eol=lf
