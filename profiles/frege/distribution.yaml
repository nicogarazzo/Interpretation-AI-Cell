name: frege
version: 1.0.0
description: "Translation critic — Sinn/Bedeutung calibration, tone and register preservation"
hermes_requires: ">=0.12.0"
author: "Nicolas Calderon"
license: "MIT"
env_requires:
  - name: ANTHROPIC_API_KEY
    description: "Anthropic API key for Claude models (production)"
    required: false
  - name: GLM_API_KEY
    description: "z.ai API key for GLM models (test)"
    required: false
